; PlatformIO project for the Bridge firmware (Serial <-> LoRa translator).
; See ARCHITECTURE.md §7.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
lib_deps =
    symlink://../common
build_flags =
    -std=gnu++17
