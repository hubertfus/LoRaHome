{
  "name": "@lorahome/tools",
  "version": "0.1.0",
  "private": true,
  "description": "Engineering tooling: metric parsing, budget/regression gating, release notes.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm run build && node --test dist/test/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0"
  }
}
